 "use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Reset(){const[s,setS]=useState("");return <main className="mx-auto max-w-md px-6 py-20"><div className="rounded-3xl border bg-white p-8"><h1 className="text-3xl font-semibold">Choose a new password</h1><form className="mt-8 space-y-4" onSubmit={async e=>{e.preventDefault();const p=String(new FormData(e.currentTarget).get("password"));const{error}=await createClient().auth.updateUser({password:p});setS(error?.message||"Password updated.");}}><input name="password" type="password" minLength={8} className="w-full rounded-xl border p-3" placeholder="New password" required/><button className="w-full rounded-xl bg-neutral-900 p-3 text-white">Update password</button></form>{s&&<p className="mt-4 text-sm">{s}</p>}</div></main>}