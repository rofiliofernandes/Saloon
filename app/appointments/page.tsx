import {createClient} from "@/lib/supabase/server";
export default async function Appointments(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return <main className="mx-auto max-w-3xl px-6 py-20"><h1 className="text-3xl font-semibold">Please sign in.</h1></main>;const{data}=await s.from("appointments").select("id,start_time,end_time,price,status,services(name),stylists(name)").eq("customer_id",user.id).order("start_time",{ascending:false});return <main className="mx-auto max-w-5xl px-6 py-16"><h1 className="text-4xl font-semibold">My appointments</h1><div className="mt-8 space-y-4">{(data??[]).map((a:any)=><div key={a.id} className="rounded-2xl border bg-white p-5"><div className="flex justify-between"><b>{a.services?.name}</b><span className="capitalize">{a.status}</span></div><p className="mt-2 text-sm text-neutral-600">{a.stylists?.name} · {new Date(a.start_time).toLocaleString()}</p><p className="mt-1 font-medium">₹{a.price}</p></div>)}</div></main>}